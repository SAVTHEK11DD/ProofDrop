{
  "name": "proofdrop",
  "version": "0.1.0",
  "private": true,
  "description": "ProofDrop v1 - file authenticity proofs with local hashing and an Ethereum-compatible blockchain anchor.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "sync:frontend": "node scripts/update-frontend-config.js sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.26.0"
  }
}
